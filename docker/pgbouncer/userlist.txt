;; PgBouncer auth file for local development
;; Plain-text password required for SCRAM-SHA-256 backend auth (PostgreSQL 16+).
;; PgBouncer performs SCRAM negotiation with the backend using this password.
;; Production userlist.txt is generated by config.sh with actual credentials.
"shop_user" "changeme"
