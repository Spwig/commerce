"""Test package for the ``vouchers`` app.

Replaces the former one-line ``vouchers/tests.py`` stub. Note that
``pytest.ini`` sets ``python_files = tests.py test_*.py *_tests.py``, so the
stub and this package cannot coexist — ``vouchers.tests`` would be ambiguous.
"""
